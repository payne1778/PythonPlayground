# typer_test

I wanted to try out this CLI library called `Typer`.
I think it will be a great tool to have in my Translation Library project in Python.

```bash
python -m venv .venv
source .venv/bin/activate
pip install typer
python typer_test/typer_test.py
```
